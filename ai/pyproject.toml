[project]
name = "spaceup-ai"
version = "0.1.0"
description = "SpaceUP AI Microservice for Floorplan Analysis and Segmentation"
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
